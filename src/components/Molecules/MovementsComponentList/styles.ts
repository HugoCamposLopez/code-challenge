import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: { gap: 20, height: 360 },
  scrollViewContainer: {
    backgroundColor: 'white',
    paddingHorizontal: 10,
    paddingTop: 23,
    borderRadius: 10,
    elevation: 1,
  },
})
